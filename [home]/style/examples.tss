.example-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 20px;
}

.example-grid div {
    padding: 15px;
    border-radius: 10px;
    background: #ffffff;
    border: 1px solid #e5e7eb;
}
