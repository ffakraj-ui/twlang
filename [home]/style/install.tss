.install-grid {
    display: grid;
    gap: 20px;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
}

.install-card {
    padding: 20px;
    border: 1px solid #e5e7eb;
    border-radius: 10px;
}
